
import React from "react";


export const revalidate = 1;

type Props = {
  params: { slug: string };
};

export default async function Page({ params }: Props) {



  return (
    <React.Fragment>

    </React.Fragment>
  );
}
